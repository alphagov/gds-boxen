# -*- mode: sh; -*-
[[ -f /opt/boxen/env.sh ]] && . /opt/boxen/env.sh

[[ -d /opt/boxen/homebrew/share/python ]] && path=($path /opt/boxen/homebrew/share/python)

# hub is too slow - this is aliased in opt/boxen/env.sh, so we need to undo it afterwards
unalias git
